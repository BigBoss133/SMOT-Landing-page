# Audit 2026-05-13

## Stato Repo: SMOT-Landing-page (BigBoss133/SMOT-Landing-page)

> Repo su GitHub — audit remoto

---

## Problemi Aperti

### CRITICI
- **JWT_SECRET hardcoded** — `auth.ts:6` ha `'dev-secret-change-me'` — **devi usare env var + validazione**
- **CORS wildcard** — `cors({ origin: '*' })` in produzione — **devi limitare ai domini autorizzati**
- **API URL hardcoded localhost** — `api.ts:1` ha `http://localhost:3000/api` — **non funziona in produzione**

### ALTI
- **localStorage per JWT token** — `AuthContext.tsx` salva token in localStorage (vulnerabile a XSS) — **usa httpOnly cookie**
- **STRIPE_SECRET_KEY non validato** — `payments.ts:7`假设 env var esiste senza check
- **Nessun test** — zero test file (né Vitest né Playwright)

### MEDI
- **Nessun .env validation** — env vars usate senza check a runtime
- **Nessun ESLint/Prettier** — code style non enforced

---

## Prossimi Passi

Vedi `TASK-ASSIGNMENT.md` per la suddivisione del lavoro per 3 persone.