# SMOT Landing — Task Salvatore (Frontend React) ✅ COMPLETATO

> Tutti i 10 task WEB sono stati implementati.

---

## Riepilogo

| Task | Stato | File |
|------|-------|------|
| **WEB-1** | ✅ | Struttura Vite + React + TS + routing (6 route) in `App.tsx` |
| **WEB-2** | ✅ | `RegisterPage.tsx` — form con validazione + POST `/api/auth/register` |
| **WEB-3** | ✅ | `LoginPage.tsx` — form login + redirect dashboard |
| **WEB-4** | ✅ | `AuthContext.tsx` — JWT localStorage, login/logout, route protection |
| **WEB-5** | ✅ | `PricingPage.tsx` — Monthly 9.99, Annual 89.99, badge 7gg gratis |
| **WEB-6** | ✅ | `CheckoutPage.tsx` — redirect Stripe + callback session_id |
| **WEB-7** | ✅ | `DashboardPage.tsx` — chiave, stato, scadenza, storico pagamenti |
| **WEB-8** | ✅ | `api.ts` — register, login, getLicenseStatus, createCheckout |
| **WEB-9** | ✅ | Tema coerente: palette SMOT in `index.css` |
| **WEB-10** | ✅ | Responsive: hamburger menu mobile + media queries |

### Build
```bash
npm run build  # ✅ 253 KB JS, 7.7 KB CSS
```
